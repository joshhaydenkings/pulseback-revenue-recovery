import { z } from 'zod';
const schema=z.object({type:z.enum(['authentication_failure','insufficient_funds','bank_timeout','late_authorization','payment_captured','payment_link_paid','payment_link_error','repeated_failure','high_value_failure','exhausted_contact_limit']),caseId:z.string().optional()});
export async function POST(request:Request){try{const input=schema.parse(await request.json());return Response.json({ok:true,simulated:true,eventId:`evt_sim_${Date.now()}`,caseId:input.caseId??(input.type==='high_value_failure'?'RC-1048':input.type==='late_authorization'?'RC-1042':'RC-1039'),type:input.type,message:input.type==='payment_link_error'?'Recovery action failed safely. No duplicate customer-facing action was created.':'Simulated event entered the PulseBack recovery pipeline.'});}catch{return Response.json({error:'Invalid simulated event'},{status:400});}}
