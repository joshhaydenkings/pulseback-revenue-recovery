import { Download, ShieldCheck } from 'lucide-react';import { AppShell } from '../../components/app-shell';import { AuditTable } from '../../components/interactive/audit-table';import { PageHeader } from '../../components/page-header';import { auditEvents } from '../../lib/demo-data';
export default function Audit(){return <AppShell active="Audit Trail"><PageHeader eyebrow="Traceable by design" title="Audit Trail" description="Every event, recommendation, authorization and action — readable and inspectable." actions={<button className="secondary-button"><Download size={14}/>Export JSON</button>}/><div className="audit-trust"><ShieldCheck size={18}/><div><b>Immutable-style history is active</b><span>AI explanations are merchant-facing summaries. Hidden model reasoning is never stored or exposed.</span></div><em>42 events today</em></div><AuditTable events={auditEvents}/></AppShell>}
