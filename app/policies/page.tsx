import { AppShell } from '../../components/app-shell';import { PageHeader } from '../../components/page-header';import { PolicyForm } from '../../components/interactive/policy-form';import { getRecoveryRepository } from '../../repositories/recovery-repository';
export default async function Policies(){const policies=await getRecoveryRepository().getPolicies();return <AppShell active="Policies"><PageHeader eyebrow="Deterministic authorization layer" title="Guardian Policies" description="Define the exact boundaries between AI recommendations and financial actions."/><PolicyForm initialPolicies={policies}/></AppShell>}
